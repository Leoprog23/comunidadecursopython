from ComunidadeImpressionadora import app

# Garantindo que o site irá rodar
if __name__ == "__main__":
    app.run(debug=True) # Todas as alterações que fizermos no código, serão enviadas para interface do site se passar o parâmetro "debug=True"